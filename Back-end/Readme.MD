#Simple example using node express and Mongo

Try visiting ```/``` and ```/index_jquery``

You can load the data using:

```mongoimport --db node_mongo --collection tweets --drop --file tweets.json```